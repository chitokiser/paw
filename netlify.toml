[build]
  # 정적 파일(C2E/index.html 등)이 들어 있는 폴더
  publish = "C2E"
  command = ""

[functions]
  # Netlify Functions 폴더
  directory = "netlify/functions"

# /api/* → Functions 라우팅
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
